/**
 * Punto de entrada de respaldo para Expo Router.
 *
 * La entrada activa se declara en package.json como `expo-router/entry`; este
 * archivo se mantiene alineado para no volver a registrar el antiguo App.tsx.
 */
import 'expo-router/entry';
