/** KYC Avanzado — Placeholder: Fase 5 */
import { View, Text } from 'react-native';
import { TW } from '@constants/theme';
export default function KycAdvancedScreen() {
  return (
    <View className={`${TW.screen} items-center justify-center`}>
      <Text className="text-text-secondary">KYC Avanzado — Fase 5</Text>
    </View>
  );
}
